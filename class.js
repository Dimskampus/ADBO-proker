class Karyawan {
    constructor(nsms, jabatan, gsji) {
    this.nama = nama;
    this.jabatan = jabatan;
    this.gaji = gaji;
  }

info() {
    return console.log(
        `$(this.nama) bekerja di perusahaan dengan jabatan $(this.jabatan) dengan gaji ${this.gaji}`);
    }
}

const karyawan = new karyawan("Dimas", "orang biasa", 100);
karyawan.info();

class
    